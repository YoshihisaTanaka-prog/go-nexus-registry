dn: ${LDAP_ROOT}
objectClass: top
objectClass: dcObject
objectClass: organization
o: Example Corporation
dc: ${LDAP_ROOT_DC}

# ==============================
# 組織単位構成
# ==============================
dn: ou=service_accounts,${LDAP_ROOT}
objectClass: organizationalUnit
ou: service_accounts

dn: ou=users,${LDAP_ROOT}
objectClass: organizationalUnit
ou: users

dn: ou=groups,${LDAP_ROOT}
objectClass: organizationalUnit
ou: groups

# ==============================
# Bind Accounts
# ==============================
dn: cn=${LDAP_BIND_CN_NEXUS},ou=service_accounts,${LDAP_ROOT}
objectClass: simpleSecurityObject
objectClass: organizationalRole
cn: ${LDAP_BIND_CN_NEXUS}
description: Bind account for Nexus LDAP authentication
userPassword: ${LDAP_BIND_PASS_NEXUS_HASH}

dn: cn=${LDAP_BIND_CN_GO},ou=service_accounts,${LDAP_ROOT}
objectClass: simpleSecurityObject
objectClass: organizationalRole
cn: ${LDAP_BIND_CN_GO}
description: Bind account for Go application LDAP queries
userPassword: ${LDAP_BIND_PASS_GO_HASH}